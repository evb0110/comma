# CoMMA Converter Rules

- Use pnpm for this project.
- Extract encoded resource URLs from CoMMA viewer URLs.
- Name output files as `[Repository] [ShelfMark] [Date Range].[ext]`.
- Use Noto Serif for medieval Latin PDF output.
- Prefer XML or DOCX when exact text fidelity matters.
